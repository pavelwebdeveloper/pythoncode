def my_insertion_sort(list):
  for i in range(1, len(list)):
     while(i!=0 and list[i]<list[i-1]):
       swap=list[i]
       list[i]=list[i-1]
       list[i-1]=swap
       i=i-1
  return list

print(my_insertion_sort([2,7,1,-4,0,2,7,4,5]))

print(my_insertion_sort([9,3,0,-4,7,1,2,-8,8,5]))
